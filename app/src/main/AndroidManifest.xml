<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mp08_projecte_final">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Mp08_projecte_final">
        <activity
            android:name=".managers.TypeManager"
            android:label="@string/title_activity_type_manager"
            android:theme="@style/Theme.Mp08_projecte_final.NoActionBar"></activity>
        <activity
            android:name=".managers.ZoneManager"
            android:label="@string/title_activity_zone_manager"
            android:theme="@style/Theme.Mp08_projecte_final.NoActionBar" />
        <activity
            android:name=".managers.MachineManager"
            android:label="@string/title_activity_machine_manager"
            android:theme="@style/Theme.Mp08_projecte_final.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>